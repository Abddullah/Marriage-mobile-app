import React from 'react';
import {View, Text} from 'react-native';

const Accepts = ({item}) => {
    return (
        <View>
            <Text>{item}</Text>
        </View>
    );
};

export default Accepts;
