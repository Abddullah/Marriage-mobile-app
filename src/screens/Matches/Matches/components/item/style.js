import {StyleSheet} from 'react-native';
export const localStyle = StyleSheet.create({
    matchProfileWrapper: {
        borderRadius: 45,
        overflow: 'hidden',
    },
    matchImage: {
        width: 60,
        height: 60,
        borderRadius: 60 / 2,
    },
    matchImageWrapper: {
        height: 90,
        width: 90,
    },
});
